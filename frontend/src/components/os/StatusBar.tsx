'use client';

// Status bar content has been merged into the Footer component.
// This export is kept so existing imports across all pages don't break.
export function StatusBar() {
  return null;
}
